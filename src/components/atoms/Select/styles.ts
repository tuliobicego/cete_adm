import styled from 'styled-components'

export const Drop = styled.select`
    width: 100%;
    background-color:#BBBBBB;
    padding: .72em .5em;
    border-radius: .5em;
    border: none;
    cursor: pointer;
    margin: 10px;
`